{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "approval",
  "description": "Least-privilege capability set for the compact approval window.",
  "windows": ["approval"],
  "permissions": [
    "core:event:allow-listen",
    "core:event:allow-unlisten",
    "core:window:allow-hide"
  ]
}
