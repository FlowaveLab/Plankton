{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "password-change",
  "description": "Least-privilege capability set for the password change confirmation window.",
  "windows": ["password-change"],
  "permissions": ["core:window:allow-hide"]
}
